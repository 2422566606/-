#include <stdio.h>
int cmp(const void*e1, const void*e2)
{
  return (int*)e1 - (int*)e2;
}
void print( void arr[], int sz)
{
  int i = 0;
  for(i=0; i<sz; i++)
  {
    printf("%d\n",arr[i]);
  }
}
int main()
{
  int arr[] = { 9, 3, 5, 6, 8, 7, 0, 1, 2, 4 };
  int sz1 = sizeof(arr)/sizeof(arr[0]);
  char str[] = { c, d, a, b, f, e };
  int sz2 = sizeof(str)/sizeof(srt[0]);
  qsort( arr, sz1, sizeof(arr[0]), cmp)
  print(arr, sz);
  return 0;
  
}
